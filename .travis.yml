language: csharp
solution: LargeList.sln

install:
- nuget restore LargeList.sln
- nuget install NUnit -Version 3.8.1 -OutputDirectory testrunner
- nuget install NUnit.ConsoleRunner -Version 3.7.0 -OutputDirectory testrunner

script:
- msbuild /p:Configuration=Travis LargeList.sln
- cp ./testrunner/NUnit.3.8.1/lib/net45/nunit.framework.dll ./LargeList/bin/x64/Release/
- mono ./testrunner/NUnit.ConsoleRunner.3.7.0/tools/nunit3-console.exe --trace=Debug --labels=All ./LargeList/bin/x64/Release/Test-LargeList.dll

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: ./LargeList/bin/x64/Release
  target_branch: gh-pages
  email: "github-pages-deploy@travis-ci.org"
  on:
    branch: master
